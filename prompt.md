FYI, ACIM Daily Minute Youtube ID is UCYDbMiDAB-29tfbyGgcg59w

Open Claude Code in a new terminal in ~/acim-daily-minute/
Drop the plan in — either paste it or reference it as a file
Say: "Build this project exactly as described in the plan, in the order specified in the Build Order section"

The plan gives Claude Code everything it needs — exact file names, folder structure, SQLite schema, function signatures, ffmpeg commands, the ElevenLabs voice ID, the YouTube OAuth pattern from JTF News, the launchd plist, and a clear build order so nothing is built out of sequence.
A few things you'll need to have ready before Claude Code's first run:

Your ELEVENLABS_API_KEY for the .env
A 1920×1080 background.jpg in assets/ (or it'll fall back to dark purple)
The 4 ACIM PDFs copied into pdfs/
The new "ACIM Daily Minute" YouTube channel created



